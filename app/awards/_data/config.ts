// Shared game config. The code is a "you're in the right place" gate for the
// live event, not real security — the host reads it aloud / drops it in Zoom
// chat, players type it at getcampfire.com/awards to join.
export const GAME_CODE = "CAMPFIRE";
export const JOIN_PATH = "/awards";
