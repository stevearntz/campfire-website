import AttuneApp from "./_components/AttuneApp";

// Self-contained Attune assessment prototype. Metadata + noindex live in
// ./layout.tsx (which also loads the Attune fonts).
export default function AssessmentPage() {
  return <AttuneApp />;
}
